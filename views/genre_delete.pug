extends layout 


block content 
  h1 #{title}: #{genre.name}

  if books_using_genre
    p You must remove these books using this genre before you can delete this genre 
    ul
      for book in books_using_genre 
        li
          a(href=book.url) #{book.title}
  else 
    p Do you really want to delete this genre?

    form(method='POST' action='')
      div.form-group
        input#genreid.form-control(type='hidden', name='genreid', required='true', value=genreid)

      button.btn.btn-primary(type='submit') Delete